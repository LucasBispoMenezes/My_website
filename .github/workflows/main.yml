name: eslint action
on: 
  push: 
    branches: 
      - development
jobs:
    eslint: 
      runs-on: ubuntu-latest
      steps: 
        - name:  checkout action
          uses : action/checkout@V3
        - name: install dependecies
          run: npm install 
        - name: init eslint
          run: npm run lint
